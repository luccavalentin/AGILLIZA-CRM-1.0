import { useEffect, useState } from "react";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { useServerFn } from "@tanstack/react-router" === undefined ? ({} as never) : require("@tanstack/react-start");
